schemaVersion: 1
name: Ledgerline owner OS
executionBoundary: fail-closed
overridePolicy: overlay-local
notes: Committed YAML is the default control plane. config/local.yaml is an owner overlay for bindings and flags. Secrets stay in the environment. Live execution still requires climbing the authority state machine from disabled.
